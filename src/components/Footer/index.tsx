import { CustomFooter } from "./CustomFooter.js";

const Footer = () => {
  return (
    <CustomFooter>
      <footer>
        <p>
          <span>React + TS + MUI </span>@2023
        </p>
      </footer>
    </CustomFooter>
  );
};

export default Footer;
